main:
  tasks:
    - setup:dev

setup:dev:
  - pip install -e .[dev]

register:
  - ./setup.py register

release:
  - pkg_version=1.0.0 ./setup.py sdist bdist_wininst upload

tests:
  - python -m pytest .